/** Matches exactly 40 lowercase hexadecimal characters in a full Git commit SHA. */
export const COMMIT_PATTERN = /^[0-9a-f]{40}$/;

/** Defines the repository-relative source-path boundary under packages/tomls/src/. */
export const SOURCE_PREFIX = 'packages/tomls/src/';
